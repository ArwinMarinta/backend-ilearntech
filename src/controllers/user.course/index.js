const userCourse = require("./user.course.controller");
const userWishlist = require("./user.whislist.controller");
const checkController = require("./check.controller");

module.exports = {
  userCourse,
  userWishlist,
  checkController,
};
